# Git demo repositories

Allowlisted roots for `POST /api/v1/evolution/git`.

Seed the bundled demo:

```bash
./scripts/seed-demo-evolve.sh
```

This creates `demo-evolve/` with three commits of `sample.txt`.
