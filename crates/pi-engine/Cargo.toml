[package]
name = "pi-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic π digit source for GENOMA DNA generation"

[dependencies]
genoma-core = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
